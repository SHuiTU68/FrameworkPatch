# FKTee-rs 白名单（allow list）
#
# 白名单模式下，仅对列在此文件中的应用进行 attestation 伪造。
# 未列出的应用保持真实硬件证书（透传原始 attestation）。
#
# 用途：仅对你指定的应用开启伪造，其余应用不受影响。
# 例如要过某个银行 App 的 attestation 检测，把它的包名加到这里。
#
# 每行一个包名，# 开头的行与空行忽略。WebUI「白名单」标签会直接编辑此文件。
# 此文件优先级高于 injector.toml 里的 [hook].allow_packages。
#
# 示例：
# com.example.banking
# com.google.android.gms